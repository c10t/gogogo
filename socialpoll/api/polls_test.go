package main

import (
	"testing"
)

func TestGet(t *testing.T) {
	t.Logf("Test not implemented")

	// mockServer := httptest.NewServer(http.HandlerFunc(handlePolls))
	// defer mockServer.Close()

	// r, err := http.Get(mockServer.URL + "/people/")

	// if err != nil {
	// 	t.Logf("mocking now: %v, %v", r, err)
	// }
}
